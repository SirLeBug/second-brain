### Commands For Jump Servers

essentials commands from SirBug's Jump Servers:

/bcheck = checks for bounces
/syncr = sync assist
/ssa = speedshot assist
/ja_help = a bunch of commands usefull for rocket jumping (optional)